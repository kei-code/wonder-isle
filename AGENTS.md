# Codex プロジェクト設定

このプロジェクトでは、PC全体の環境汚染を避けるため、依存関係や実行環境の扱いに以下のルールを適用する。

## Python 環境管理

- Python パッケージのインストールには必ず `uv` を使う。
- `pip install` は使わない。
- Python スクリプトの実行は `uv run python` を使う。
- Python CLI ツールのインストールは `uv tool install` を使う。
- プロジェクト依存関係を追加する場合は `uv add` を使う。

## グローバル環境への変更禁止

以下の操作は、ユーザーの明示的な許可なしに実行しない。

- `npm install -g` などのグローバル npm インストール
- `pip install` による直接インストール
- `brew`、`winget`、`choco` などによるシステムへのツール追加
- PATH や環境変数の変更
- システム設定ファイルの変更
- 複数プロジェクトに影響しうる依存関係の変更
- バージョンの上書き、固定、ダウングレード

## 確認が必要な場合の説明フォーマット

環境に影響する操作が必要になった場合は、実行前に日本語で以下の形式で説明し、ユーザーに確認する。

```text
⚠️ 環境への影響があります

【何をしようとしているか】
〇〇をインストールしようとしています。

【影響範囲】
グローバル環境 / このプロジェクト のみ

【環境汚染のリスク】
〇〇という理由で、他のプロジェクトに影響する可能性があります。

【推奨する代替手段】
uv を使って分離された環境にインストールすることを推奨します。

実行してよいですか？
```

## Node.js / フロントエンド開発

- 依存関係は原則としてこのプロジェクト内に閉じる。
- グローバル npm パッケージは使わず、必要なツールはプロジェクト依存または `npx` / `npm exec` 相当で扱う。
- 開発サーバー起動やビルドは、既存の `package.json` スクリプトを優先する。

## 開発方針

- まず小さく動くものを作り、公開しやすい構成を保つ。
- ゲームは1本ずつ独立して追加できる構成にする。
- 環境構築よりも、静的サイトとして公開しやすい実装を優先する。
- サイト全体は、自然の生態が異なる複数の島を探索しながらゲームに出会う構成を基本方針とする。
- ユーザーへの提案・レビューでは忖度をしない。良い物を作るために、有用な意見、懸念、代替案を遠慮なく率直に伝える。
